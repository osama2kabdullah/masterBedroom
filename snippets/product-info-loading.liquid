{%- liquid
    assign NS = 'product-info-loading'
    assign cn = '#' | append: NS    
-%}

{% render 'load-css', file: NS %}

<{{ NS }} class="{{ cn }}" section-id={{ section.id }}>
    {% render 'spinner' %}
</{{ NS }}>

<script type="module" src="{{ NS | append: '.js' | asset_url }}"></script>